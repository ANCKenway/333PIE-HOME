# 333HOME Agents - Requirements

# Core dependencies
websockets>=12.0
pydantic>=2.0
psutil>=5.9.0

# Windows-specific (TITO - LogMeIn automation)
selenium>=4.15.0; sys_platform == 'win32'
webdriver-manager>=4.0.0; sys_platform == 'win32'
pywinauto>=0.6.8; sys_platform == 'win32'

# Linux-specific (333srv - SSH/Docker control)
paramiko>=3.3.0; sys_platform == 'linux'
asyncssh>=2.14.0; sys_platform == 'linux'
docker>=6.1.0; sys_platform == 'linux'

# Optional (deployment)
pyinstaller>=6.0.0  # For standalone executables
